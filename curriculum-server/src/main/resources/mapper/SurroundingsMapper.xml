<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.curriculum.mapper.SurroundingsMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.curriculum.model.po.Surroundings">
        <id column="id" property="id" />
        <result column="title" property="title" />
        <result column="price" property="price" />
        <result column="brand" property="brand" />
        <result column="ip" property="ip" />
        <result column="vip_price" property="vipPrice" />
        <result column="tags" property="tags" />
        <result column="pic" property="pic" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, title, price, brand, ip, vip_price, tags, pic
    </sql>

</mapper>
